SELECT ID, user_login, user_email, display_name, M1.meta_value AS local_area, M2.meta_value AS local_group
FROM `wp_users` U, `wp_usermeta` M1, `wp_usermeta` M2
WHERE U.ID = M1.user_id AND M1.meta_key = 'local_area'
  AND U.ID = M2.user_id AND M2.meta_key = 'local_group'
ORDER BY user_email
